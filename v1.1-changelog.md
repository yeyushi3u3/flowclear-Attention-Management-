---
name: flowclear-v1.1
description: 静流 V1.1 版本 — 红色Toast通知、消息分类、滑动删除、全页面暗色
metadata:
  type: project
---

# 静流 (FlowClear) — V1.1 存档点

**存档日期**: 2026-06-01
**版本**: V1.1 (在 V1.0 MVP Demo 基础上迭代)
**Git commit**: `2b1366d`
**位置**: `d:\xinxizhengli\1.2\`

## V1.1 新增功能

### 1. 红色消息顶部悬浮通知 (5秒)
- 新增 [RedMessageToast.tsx](../src/components/dashboard/RedMessageToast.tsx)
- 未读红色消息到达时从**页面顶部滑入**，`z-60` 层级高于一切
- 停留 **5秒** 后自动消失，带白色进度条倒计时
- 红色渐变背景 (`from-red-600 to-rose-600`)，脉冲图标强调
- 可点击关闭按钮手动消除，消除后标记为已读

### 2. 消息分类标签
- 类型定义新增 `MessageCategory`：work / study / entertainment / shopping / social / finance / system / life
- 每条消息新增 `category` 字段，对应中文标签 + 彩色徽章
- 所有 18 条 mock 消息和 3 条 tutorial 消息均已分配分类
- 标签显示在卡片底部、时间戳左侧，一目了然

### 3. 左滑删除 (类似QQ消息交互)
- [MessageCard.tsx](../src/components/dashboard/MessageCard.tsx) 支持 Pointer Events 拖拽
- **左滑超过 80px** 露出红色"删除"按钮区域
- 点击删除按钮触发确认动画，完成后从列表移除
- 露出删除后展示"取消"按钮可恢复
- 鼠标 hover 时显示"← 左滑删除"提示

### 4. 全页面暗色模式
- CSS 增强：`html.dark` + `body` + `#root` 全部变暗
- 设置 `color-scheme: dark` 确保浏览器 UI 也切换
- 背景过渡动画 `0.3s ease` 平滑切换

**Why:** 用户反馈原始暗色模式覆盖不彻底，部分背景仍为亮色导致刺眼。
**How to apply:** 通过右上角 🌙/☀️ 按钮一键切换，状态持久化到 localStorage。

## 项目结构 (V1.1)

新增文件:
- `src/components/dashboard/RedMessageToast.tsx`

修改文件:
- `src/types/index.ts` — 新增 `MessageCategory` 类型
- `src/data/mockMessages.ts` — 18条 mock 添加 category
- `src/data/tutorialMessages.ts` — 3条教程添加 category
- `src/stores/useMessageStore.ts` — 新增 `deleteMessage`
- `src/components/dashboard/MessageCard.tsx` — 滑动删除 + 分类标签
- `src/components/dashboard/KanbanView.tsx` — 透传 onDelete
- `src/components/dashboard/TimelineView.tsx` — 透传 onDelete
- `src/pages/DashboardPage.tsx` — 集成 RedMessageToast
- `src/index.css` — 全页面暗色覆盖

## 版本回退

```bash
cd d:/xinxizhengli/1.2
git log --oneline           # 查看所有存档点
git reset --hard 2b1366d    # 回到 V1.1
git reset --hard a821fa5    # 回到 V1.0 MVP Demo
```

[[flowclear-project-overview]]
[[flowclear-v1.2]]
