---
name: flowclear-v1.2
description: 静流 V1.2 版本 — 夜间模式修复、模拟消息平衡、规则保存修复、看板计数即时更新
metadata:
  type: project
---

# 静流 (FlowClear) — V1.2 存档点

**存档日期**: 2026-06-01
**版本**: V1.2 (在 V1.1 基础上修复 4 个问题)
**Git commit**: `218b6ad`
**位置**: `d:\xinxinzhengli\1.2\`

## V1.2 修复清单

### 1. 夜间模式全面修复
- **根因**：Tailwind v4 的 `dark:` 变体默认监听 `prefers-color-scheme` 系统偏好，不响应手动 `.dark` class；CSS 变量 `--color-zen-bg` 固定为亮色值不变
- **修复**：
  - `:root` 定义亮色值，`.dark` 定义暗色值覆盖同名变量 → `var(--color-zen-bg)` 自动切换
  - 添加 `@variant dark (&:where(.dark, .dark *))` 使 Tailwind `dark:` 类名响应 class 切换

### 2. 模拟消息平衡（蓝/黄消息缺失）
- **根因**：身份预设的蓝/黄规则关键词（如`周报`、`团建`）与 mock 池不匹配，模拟时只能命中红/灰
- **修复**：新增 4 条针对性 mock 消息，确保能命中蓝/黄规则关键词

### 3. 规则保存按钮无法点击
- **根因**：`disabled={!isValid}` 在特定条件下阻止点击，`<form>` 表单提交与关键词输入 Enter 事件相互干扰
- **修复**：去掉 `disabled` 属性和 `<form>`，按钮永远可点击，验证逻辑纯在点击处理器中执行，不满足时显示红色错误提示

### 4. 看板视图删除消息后计数不更新
- **根因**：删除依赖 `onAnimationComplete` 回调才调用 `onDelete`，动画 0.3s 期间 store 不更新，计数不刷新
- **修复**：点击删除按钮立即 `onDelete`（store 即时更新 → 计数即时刷新），`AnimatePresence` 从 MessageCard 移到列表层自动处理退出动画

## 完整版本线

```bash
git log --oneline
# 218b6ad V1.2: 删除计数即时更新 + AnimatePresence重构
# 59d7549 模拟消息增加蓝黄触发消息 + RuleForm改用表单修复保存按钮
# c1e1ebb 根治夜间模式 — CSS变量切换+class-based dark variant
# 2b1366d V1.1: 红色Toast通知 + 消息分类标签 + 滑动删除 + 全页面暗色
# a821fa5 V1.0: MVP Demo 初始版本
```

```bash
# 回退命令
git reset --hard 218b6ad  # V1.2（最新）
git reset --hard 2b1366d  # V1.1
git reset --hard a821fa5  # V1.0
```

[[flowclear-project-overview]]
